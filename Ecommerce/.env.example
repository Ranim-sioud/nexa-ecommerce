# API base URL — must point to the backend /api prefix.
# In production this is your domain proxied by nginx (e.g. https://nexa-tn.com/api).
# In local development leave as default (http://localhost:4001/api).
VITE_API_URL=http://localhost:4001/api
